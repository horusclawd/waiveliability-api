# Copy to .env and fill in values for local development

# Database
POSTGRES_DB=waiveliability
POSTGRES_USER=waiveliability
POSTGRES_PASSWORD=changeme

# Redis
REDIS_PASSWORD=changeme

# JWT
JWT_SECRET=change-me-to-a-256-bit-secret-for-local-dev

# AWS / LocalStack (local dev uses LocalStack — no real credentials needed)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1
S3_BUCKET=waiveliability-local
S3_ENDPOINT=http://localhost:4566

# Stripe (use test keys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Mail (use MailHog locally)
MAIL_HOST=localhost
MAIL_PORT=1025
