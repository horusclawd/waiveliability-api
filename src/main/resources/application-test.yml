spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:waiveliability_test}
    username: ${DB_USER:waiveliability}
    password: ${DB_PASSWORD:changeme}

  flyway:
    enabled: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

logging:
  level:
    root: WARN
    com.waiveliability: INFO

app:
  aws:
    region: us-east-1
    s3:
      bucket: waiveliability-test
      endpoint: ""
  stripe:
    secret-key: ""
    webhook-secret: ""
  jwt:
    secret: test-secret-must-be-at-least-256-bits-long-for-hmac-sha256!!!
    access-token-expiry: 900
    refresh-token-expiry: 604800
